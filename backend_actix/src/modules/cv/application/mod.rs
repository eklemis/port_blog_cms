/// The snapshot handles the route layer resolves through `AppState`.
pub mod cv_snapshot_use_cases;
pub mod ports;
/// Implementations of this module's use-case contracts.
pub mod services;
pub mod use_cases;
