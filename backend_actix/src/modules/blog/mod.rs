/// Adapters: the layer that talks to the outside world in both directions.
pub mod adapter;
/// The application layer: ports, use cases, services and domain types.
pub mod application;
/// Domain types: the vocabulary the business rules are written in.
pub mod domain;
