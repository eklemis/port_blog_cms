/**
 * The product's name, as a person reads it.
 *
 * One definition, because it is on every screen: the wordmark in the auth
 * header today, and page titles and transactional copy after that. Rename it
 * here and every surface follows — a product name spelled out in markup is the
 * kind of thing that gets renamed in nine places out of ten.
 *
 * The Figma frames draw it as "Arch". The design documents and the placeholder
 * home page call the product "Blogport", which is an inconsistency someone
 * should settle; when they do, this line is the whole change.
 */
export const PRODUCT_NAME = 'Arch';
